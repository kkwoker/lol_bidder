Welcome to lol_bidder

<%= form_tag summoners_path, method: "get" do %>
	<%= text_field_tag "name" %>
	<%= submit_tag "Search" %>

<% end %>

<% @summoners.each do |summoner| %>
	
		<%= form_tag summoners_path, method: "get" do %>
			<%= hidden_field_tag "name",summoner.name %>
			<%= submit_tag summoner.name, class: "summoner-names" %>
		<% end %>
	
<% end %>